# picklist_videos

Shared MP4 source for both embedding extraction and annealing.

Expected files: `picklist_NNN.MP4` (or `.mp4`), matching stems in
`picklist_jsons/` and `picklist_labels/`.

Contents of this folder are gitignored.
